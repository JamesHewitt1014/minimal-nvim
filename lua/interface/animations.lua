require('mini.animate').setup({
	cursor = {enable = false},
	scroll = {enable = true},
	resize = {enable = true}, -- window resize
	open   = {enable = false}, -- window open
	close  = {enable = false}, -- window close
})

require('mini.notify').setup()
